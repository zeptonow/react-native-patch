name: Sonar Workflow

on:
  push:
    branches:
      - 0.71.12

  pull_request:
    branches:
      - 0.71.12

jobs:
  Sonar-Analysis:
    uses: zeptonow/workflow-hub/.github/workflows/sonar-callable-workflow.yml@main
    secrets: inherit